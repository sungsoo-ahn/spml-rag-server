# RAG Server Configuration - Dropbox Shared Version
# This config uses a shared Dropbox directory for the ChromaDB
# All team members should point to their own Dropbox sync location

# Embedding settings
embedding_model: "text-embedding-3-small"

# Chunking settings
chunk_size: 512       # tokens per chunk
chunk_overlap: 50     # overlap between chunks

# ChromaDB settings - SHARED VIA DROPBOX
# NOTE: Update this path to match YOUR Dropbox location
# Example paths:
#   macOS: /Users/YOUR_USERNAME/Dropbox/SPML/data/spml-rag-server/chroma-db
#   Windows: C:/Users/YOUR_USERNAME/Dropbox/SPML/data/spml-rag-server/chroma-db
#   Linux: /home/YOUR_USERNAME/Dropbox/SPML/data/spml-rag-server/chroma-db
chroma_db_path: "${DROPBOX_PATH}/SPML/data/spml-rag-server/chroma-db"
collection_name: "documents"

# Document settings - SHARED VIA DROPBOX
# All team members can access the same document collection
# Example paths:
#   macOS: /Users/YOUR_USERNAME/Dropbox/SPML/data/spml-rag-server/documents
#   Windows: C:/Users/YOUR_USERNAME/Dropbox/SPML/data/spml-rag-server/documents
#   Linux: /home/YOUR_USERNAME/Dropbox/SPML/data/spml-rag-server/documents
documents_path: "${DROPBOX_PATH}/SPML/data/spml-rag-server/documents"
supported_extensions:
  - ".pdf"
  - ".docx"
  - ".doc"
  - ".pptx"
  - ".ppt"
  - ".txt"
  - ".md"
  - ".markdown"

# Retrieval settings
default_top_k: 5
similarity_threshold: 0.3

# Server settings
server_name: "rag-server"
log_level: "INFO"
